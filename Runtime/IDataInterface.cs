namespace GAOS.EventSystem
{
    /// <summary>
    /// Base interface that all event parameter and return types must implement.
    /// This ensures type safety and consistent data contracts across the event system.
    /// </summary>
    public interface IDataInterface
    {
    }
} 